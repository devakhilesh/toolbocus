<%- include('../../layout/header') %>
<%- include('../../layout/sidebar') %>

<main id="main" class="main">
   <% if (permissions.includes(39)) { %> 

    <div class="text-left m-4">
        <a href="/add_estimated_sizes" class="btn btn-primary login-btn"><i class="bi bi-plus-lg"></i> Add Estimated Size</a>
    </div>

    <section class="section dashboard">
        <div class="row">
            <div class="col-12">
                <div class="card top-selling overflow-auto">
                   

                    <div class="card-body pb-0">
                        <h5 class="card-title">All Estimated Sizes</h5>
                        <div class="mb-3">
                            <button type="button" class="btn btn-danger mb-2 d-none" id="deleteSelectedBtn"><i class="bi bi-trash"></i> Delete Selected</button>
                        </div>
                        <table id="estimated_sizesTable" class="table table-hover">
                            <thead>
                                <tr>
                                    <!-- <th>
                                        <div class="form-check">
                                            <input class="form-check-input" type="checkbox" id="gridCheckAll">
                                            <label class="form-check-label" for="gridCheckAll"></label>
                                        </div>
                                    </th> -->
                                    <th scope="col">Sr No.</th>
                                    <th scope="col">Estimated Size</th>
                                    <!--  <th scope="col">Status</th>-->
                                    <th scope="col">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% let i = 1; %>
                                <% if (estimated_sizes && estimated_sizes.length > 0) { %>
                                    <% estimated_sizes.forEach(estimated_size => { %>
                                        <tr>
                                            <!-- <td>
                                                <div class="form-check">
                                                    <input class="form-check-input city-checkbox" type="checkbox" data-id="<%= estimated_size.id %>">
                                                    <label class="form-check-label" for="propertyCheck<%= estimated_size.id %>"></label>
                                                </div>
                                            </td> -->
                                            <td><%= i++ %></td>
                                            <td><%= estimated_size.name %></td>
                                             <!-- <td style="color: <%= estimated_size.status === 1 ? 'green' : 'red' %>;">
                                                 <%= estimated_size.status === 1 ? 'Active' : 'Deactive' %>
                                            </td>-->
                                            
                                            <td>
                                                <a href="/editestimated_sizes/<%= estimated_size.id %>" class="btn btn-sm btn-primary"><i class="bi bi-pencil"></i></a>
                                                <button class="btn btn-sm btn-danger estimated_size" data-id="<%= estimated_size.id %>"><i class="bi bi-trash"></i></button>
                                            </td>
                                        </tr>
                                    <% }) %>
                                    
                                <% } else { %>
                                    <tr>
                                        <td colspan="5" style="text-align: center;" class="mt-4">No Estimated Sizes found.</td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                        <% } else { %>
                  <img src="https://builder.propal.in/Images/access_denied.png" alt="No Permission" style="max-width: 100%; height: auto;" />
                  <% } %>
                    </div>
                </div>
            </div>
        </div>
    </section>
</main>

<%- include('../../layout/footer') %>

<script>
document.addEventListener('DOMContentLoaded', () => {
    const estimatedsizesTable = $('#estimated_sizesTable').DataTable({
        responsive: true
    });

    estimatedsizesTable.on('init.dt', function () {
        $('.dataTables_filter').addClass('d-none');
        $('.dataTables_length').addClass('d-none');
    });

    // Event delegation for individual Delete buttons
    $('#estimated_sizesTable').on('click', '.estimated_size', async function () {
        const id = $(this).data('id');
        try {
            const response = await fetch(`/delete_estimated_sizes/${id}`, {
                method: 'DELETE'
            });
            const data = await response.json();
            if (response.ok) {
                await Swal.fire({
                    icon: 'success',
                    title: 'Success',
                    text: 'Estimated Sizes deleted successfully',
                    timer: 1500,
                    timerProgressBar: true,
                    didClose: () => {
                        setTimeout(() => {
                            window.location.reload();
                        }, 500);
                    }
                });
            } else {
                await Swal.fire({
                    icon: 'error',
                    title: 'Error',
                    text: 'Failed to Delete Estimated Sizes.'
                });
            }
        } catch (error) {
            console.error('Error:', error);
            await Swal.fire({
                icon: 'error',
                title: 'Error',
                text: 'Failed to Delete Estimated Sizes.'
            });
        }
    });



 
});


</script>
